version: '1.0'


services:

  backend:
    image: nitin471/node-backend
    ports:
      -"3000:3000"
    depends_on:
      - mongo

  
  mongo:
    image: mongo
    ports:
    - "27017:27017"
    volumes:
    - mongodata:/data/db

volumes:
  mongodata:



